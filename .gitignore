# macOS
.DS_Store

# Virtual environment
venv/
.venv/
.venv_soml/

# Python
__pycache__/
*.pyc
*.pyo

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# AI Agents
CLAUDE.md
GEMINI.md
.claude/
AGENTS.md

# Local-only gitleaks config (contains specific literals — never commit)
.gitleaks.local.toml

# Superpowers planning artifacts (specs, plans) — kept local-only
docs/superpowers/
.superpowers/

# Work in Process folder
WIP/

# Data and trained networks
Exercises/Ex2-*/

# Reproducing Target Repo for Mini-project
# Link: https://github.com/NVlabs/DoRA#
DoRA/
Risk-Assessment-mini-project.md
2402.09353v6.pdf

# --- mini-project (mini-project-DoRA) ---
# commonsense_170k training data (downloaded on Spartan; ~tens of MB, not committed)
mini-project-DoRA/data/

# Local-only venv (uv venv on Spartan lives under the project allocation; on Mac we don't install)
mini-project-DoRA/.venv/

# Per-user env file with real site-specific paths
mini-project-DoRA/scripts/local/setup_env.sh

# Results: track small text artifacts; ignore heavy ones
mini-project-DoRA/results/_slurm/
mini-project-DoRA/results/**/adapter/
mini-project-DoRA/results/**/checkpoint-*/
mini-project-DoRA/results/**/trainer_out/
mini-project-DoRA/results/**/pytorch_model*.bin
mini-project-DoRA/results/**/*.safetensors

# Figures WIP
mini-project-DoRA/figures/_wip/

# Python (mini-project additions; __pycache__/ already covered at the top)
*.egg-info/
.pytest_cache/
